{
	"name": "whitespace-removal",
	"description": "Removes textual elements containing only whitespace as content.",
	"specs": {
		"minWidth": {
			"value": 0,
			"range": { "min": 0, "max": 100 }
		}
	}
}
